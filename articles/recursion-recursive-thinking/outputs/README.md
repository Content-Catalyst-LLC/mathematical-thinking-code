# Outputs

Generated outputs should be written here.

- `outputs/tables/`: CSV files, SQLite databases, recursive definition audits, recurrence values, tree audits, memoization effects, grammar indexes, recursive model trajectories, algorithm indexes, and warning tables
- `outputs/figures/`: Graphviz DOT files and optional generated diagrams

These outputs are reproducible artifacts created by the scripts in the language-specific folders.

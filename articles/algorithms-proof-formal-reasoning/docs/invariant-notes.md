# Invariant Notes

An invariant is a claim that remains true while execution changes state.

## Invariant proof pattern

1. Initialization: the invariant is true before the loop.
2. Preservation: one iteration preserves the invariant.
3. Termination use: when the loop stops, the invariant implies the postcondition.

Invariants identify stable truth inside procedural change.
